@{
    Layout = "_AdminLayout";
}

@* The view start file tells Razor to use a layout file in the HTML that it generates,
    because this will help to reduce the amount of duplication in views.  *@
@* create the view, add a Razor layout named _AdminLayout.cshtml to the Views/Shared folder. *@
